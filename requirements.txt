# --- core pipeline (Track A) ---
pydantic>=2.7
httpx>=0.27
python-dotenv>=1.0

# --- LLM providers ---
google-genai>=0.3          # Gemini (primary)
groq>=0.9                  # fallback provider

# --- storage (Track A) ---
sqlmodel>=0.0.22

# --- API (Track A) ---
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.1

# --- retrieval & extraction (Track B) ---
pymupdf>=1.24              # PDF -> text
rapidfuzz>=3.9             # fuzzy title dedupe AND Stage 1 span matching

# --- evaluation (Track C) ---
statsmodels>=0.14          # Fleiss' kappa
pandas>=2.2
matplotlib>=3.8

# --- dev ---
pytest>=8.2
pytest-asyncio>=0.23
